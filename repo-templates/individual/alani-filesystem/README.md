# alani-filesystem

Virtual filesystem, path resolution, mount table, inode model, journaling hooks, and file capabilities.

| Field | Value |
|---|---|
| Tier | MVK required |
| Owner | Storage and filesystem team |
| Aliases | None |
| Architectural dependencies | `alani-abi`, `alani-protocol`, `alani-storage`, `alani-policy`, `alani-audit`, `alani-observability` |

## Quick start

```bash
cargo fmt -- --check
cargo test --all-features
```

This is a starter repository generated by the Alani specification bundle. Keep public API changes synchronized with `docs/repositories/alani-filesystem.md`, Doc 42, and Doc 43.
