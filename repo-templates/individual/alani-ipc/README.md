# alani-ipc

Capability-aware message passing, ports, channels, event queues, shared-memory handles, and routing contracts.

| Field | Value |
|---|---|
| Tier | MVK required |
| Owner | Kernel and runtime teams |
| Aliases | None |
| Architectural dependencies | `alani-abi`, `alani-protocol`, `alani-policy`, `alani-observability` |

## Quick start

```bash
cargo fmt -- --check
cargo test --all-features
```

This is a starter repository generated by the Alani specification bundle. Keep public API changes synchronized with `docs/repositories/alani-ipc.md`, Doc 42, and Doc 43.
