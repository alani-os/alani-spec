# alani-package

Package format, local registry, dependency resolver, installation transaction model, and package integrity checks.

| Field | Value |
|---|---|
| Tier | Post-MVK boundary |
| Owner | Runtime and release teams |
| Aliases | None |
| Architectural dependencies | `alani-abi`, `alani-protocol`, `alani-config`, `alani-policy`, `alani-filesystem`, `alani-identity` |

## Quick start

```bash
cargo fmt -- --check
cargo test --all-features
```

This is a starter repository generated by the Alani specification bundle. Keep public API changes synchronized with `docs/repositories/alani-package.md`, Doc 42, and Doc 43.
