# alani-abi

Canonical syscall numbers, repr(C) structs, error codes, handle types, ABI versioning, and feature flags.

| Field | Value |
|---|---|
| Tier | MVK required |
| Owner | ABI owners |
| Aliases | None |
| Architectural dependencies | None |

## Quick start

```bash
cargo fmt -- --check
cargo test --all-features
```

This is a starter repository generated by the Alani specification bundle. Keep public API changes synchronized with `docs/repositories/alani-abi.md`, Doc 42, and Doc 43.
