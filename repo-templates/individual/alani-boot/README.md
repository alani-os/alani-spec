# alani-boot

UEFI and emulator boot handoff logic, kernel image metadata, boot manifest parsing, and early console support.

| Field | Value |
|---|---|
| Tier | MVK required |
| Owner | Platform team |
| Aliases | None |
| Architectural dependencies | `alani-abi`, `alani-platform`, `alani-config` |

## Quick start

```bash
cargo fmt -- --check
cargo test --all-features
```

This is a starter repository generated by the Alani specification bundle. Keep public API changes synchronized with `docs/repositories/alani-boot.md`, Doc 42, and Doc 43.
