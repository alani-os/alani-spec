# alani-config

Typed configuration schemas for boot, devices, runtime, security, corpus, release, and environment profiles.

| Field | Value |
|---|---|
| Tier | MVK required |
| Owner | Platform and runtime teams |
| Aliases | None |
| Architectural dependencies | `alani-protocol` |

## Quick start

```bash
cargo fmt -- --check
cargo test --all-features
```

This is a starter repository generated by the Alani specification bundle. Keep public API changes synchronized with `docs/repositories/alani-config.md`, Doc 42, and Doc 43.
