# alani-protocol

Shared wire and data schemas for IPC envelopes, audit events, device descriptors, config records, and corpus metadata.

| Field | Value |
|---|---|
| Tier | MVK required |
| Owner | Interface owners |
| Aliases | None |
| Architectural dependencies | `alani-abi` |

## Quick start

```bash
cargo fmt -- --check
cargo test --all-features
```

This is a starter repository generated by the Alani specification bundle. Keep public API changes synchronized with `docs/repositories/alani-protocol.md`, Doc 42, and Doc 43.
